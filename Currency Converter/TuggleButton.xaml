<UserControl x:Class="Currency_Converter.TuggleButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Currency_Converter"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
        <UserControl.Resources>
        </UserControl.Resources>
        <Grid>
            <Viewbox VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid Height="150"
                  Width="300">
                    <Rectangle x:Name="Back"
                           Margin="5"
                           Fill="#9A9A9A"
                           RadiusY="70" RadiusX="70" MouseLeftButtonDown="Back_MouseLeftButtonDown"/>
                    <Ellipse x:Name="Dot"
                         Fill="White"
                         HorizontalAlignment="Center" VerticalAlignment="Center"
                         Height="120" Width="120" MouseLeftButtonDown="Dot_MouseLeftButtonDown"/>
                </Grid>
            </Viewbox>
        </Grid>
    </UserControl>
